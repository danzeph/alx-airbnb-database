--Indexes on the user table
--user_id as our primary is automatically indexed
CREATE INDEX urole ON users(role);
CREATE INDEX ON users(created_at);


--Indexes on the user table
--user_id as our primary is automatically indexed


--Indexes on the user table
--user_id as our primary is automatically indexed
